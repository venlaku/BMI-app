import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.practice.ionicreact',
  appName: 'ionic-react',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
